// Auto-generated by Mbed CE.  Edits will be erased when CMake is rerun.
{
	"version": "2.0.0",
	"tasks": [
		// Build for target steering-wheel
		{
			"label": "Build steering-wheel",
			"type": "shell",
			"command": "C:/Program Files/CMake/bin/cmake.exe",
			"args": ["--build", "C:/Users/SidSa/FirmwareRepo/fs-4-firmware/steering-wheel/build/-debug", "--target", "steering-wheel"],
		},
		// Build steering-wheel and run the GDB server
		{
			"label": "Build steering-wheel and start GDB server",
			"dependsOn": ["Build steering-wheel", "GDB Server"],
			"dependsOrder": "sequence",
		},
		{
			"label": "GDB Server",
			"type": "shell",
			"command": "C:/Users/SidSa/FirmwareRepo/fs-4-firmware/mbed-os/venv/Scripts/python.exe",
			"args": ["-m", "pyocd", "gdbserver", "--no-wait", "-t", "STM32G441KBT6", "-f", "100k", "-p", "23331"],
			"isBackground": true,
			// This task is run to start the GDB server, so that the launch configuration can connect to it.
      		// Problem is, it's a GDB server, and since it never exits, VSCode
     	 	// will never start the debug session. All this is needed so VSCode just lets it run.
			"problemMatcher": [
				{
					"pattern": [
						{
							"regexp": "________________",
							"file": 1,
							"location": 2,
							"message": 3
						}
					],
					"background": {
						"activeOnStart": true,
						"beginsPattern": ".*",
						"endsPattern": ".*",
					}
				}
			],
		}
	]

}
	