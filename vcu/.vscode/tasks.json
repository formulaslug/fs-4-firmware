// Auto-generated by Mbed CE.  Edits will be erased when CMake is rerun.
{
	"version": "2.0.0",
	"tasks": [
		// Build for target vcu
		{
			"label": "Build vcu",
			"type": "shell",
			"command": "/opt/homebrew/bin/cmake",
			"args": ["--build", "/Users/jpins/Documents/Coding/CPP/Hardware/formula-slug/fs-4-firmware/vcu/build", "--target", "vcu"],
		},
		// Build vcu and run the GDB server
		{
			"label": "Build vcu and start GDB server",
			"dependsOn": ["Build vcu", "GDB Server"],
			"dependsOrder": "sequence",
		},
		{
			"label": "GDB Server",
			"type": "shell",
			"command": "/opt/homebrew/bin/openocd",
			"args": ["-f", "interface/stlink-dap.cfg", "-c", "transport select dapdirect_swd", "-c", "reset_config srst_only srst_nogate connect_assert_srst", "-f", "target/stm32f4x.cfg", "-c", "[lindex [target names] 0] configure -event gdb-detach {shutdown}", "-c", "gdb_port 23331"],
			"isBackground": true,
			// This task is run to start the GDB server, so that the launch configuration can connect to it.
      		// Problem is, it's a GDB server, and since it never exits, VSCode
     	 	// will never start the debug session. All this is needed so VSCode just lets it run.
			"problemMatcher": [
				{
					"pattern": [
						{
							"regexp": "________________",
							"file": 1,
							"location": 2,
							"message": 3
						}
					],
					"background": {
						"activeOnStart": true,
						"beginsPattern": ".*",
						"endsPattern": ".*",
					}
				}
			],
		}
	]

}
	